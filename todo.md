program1

loop1:

video/image -> create cvat project

manual annotation -> export to yolo format ( user / program )

train yolo model ( program )

video/image -> infer using trained yolo model ( program ) -> bounding boxes ( program )

output -> display bounding boxes on video/image ( program )

user check:

loop2:

same step, but tarin on user anomanually annotated data
